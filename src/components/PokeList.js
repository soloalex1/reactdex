import React from 'react';
import './styles/PokeList.scss';

// stateless component, no need to make it a class.
const PokeList = () => {
	return (
		<section className="pokelist">
		</section>
	)
}

export default PokeList;